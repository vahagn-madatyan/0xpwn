# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| 1 | 2026-03-12 | project | Build fresh vs fork Strix | Build from scratch, borrow patterns | Strix lacks tiered permissions, validation agent, and freemium model; forking would carry debt and constrain architecture | no |
| 2 | 2026-03-12 | project | License | Apache 2.0 | Maximum adoption + commercial flexibility; matches Strix's license | no |
| 3 | 2026-03-12 | project | Python package name | `oxpwn` (package) / `0xpwn` (CLI) | Python identifiers can't start with digit; `oxpwn` is valid, `0xpwn` is the brand CLI command | no |
| 4 | 2026-03-12 | project | LLM abstraction | LiteLLM | Provider-agnostic (100+ models), built-in cost tracking, async, tool calling — no custom wrapper needed | yes |
| 5 | 2026-03-12 | project | Default model onboarding | Guided first-run wizard | Adaptive: detect Ollama → offer local, or guide API key setup. Lower barrier than requiring config | yes |
| 6 | 2026-03-12 | project | Docker sandbox image | Custom published image (ghcr.io/0xpwn/sandbox) | Baked-in tools for reproducibility and fast cold start vs installing at runtime | yes |
| 7 | 2026-03-12 | project | Agent output verbosity | Full reasoning stream by default | Bug bounty hunters identified "watching the AI operate" as the selling moment; transparency > cleanliness | yes |
| 8 | 2026-03-12 | project | External data sources in M001 | Include NVD/CVE enrichment | Enriched findings significantly more useful for bug bounty reports; worth the added slice | yes |
| 9 | 2026-03-12 | project | Target user for launch | Bug bounty hunters | Speed-obsessed, cost-sensitive, value PoC quality — best fit for autonomous agent value prop | yes |
| 10 | 2026-03-12 | project | CLI framework | Typer + Rich | Typer for CLI structure, Rich for streaming output and formatting — well-supported, fast to build | yes |
| 11 | 2026-03-12 | S01 | Build backend | hatchling | Lightweight, native src/ layout support, no setup.py needed — simplest option for pure-Python packages | yes |
| 12 | 2026-03-12 | S01 | Token tracking model | Nested `TokenUsage` Pydantic model | Separate input/output/total as a dedicated model rather than flat fields — mirrors provider response shapes, cleaner composition | yes |
| 13 | 2026-03-12 | S01 | ScanState mutation strategy | Mutable methods (add_finding, advance_phase, etc.) | Scan sessions are inherently mutable lifecycle objects; immutable updates would add ceremony without benefit here | yes |
| 14 | 2026-03-13 | S01 | Integration test LLM provider | `gemini/gemini-2.5-flash` via GEMINI_API_KEY | Available key, free tier; gemini-2.0-flash quota exhausted. Proves provider-agnostic design by using non-default provider. Override via `OXPWN_TEST_MODEL` env var | yes |
| 15 | 2026-03-13 | S02 | Tool executor pattern | Constructor takes `DockerSandbox`, `async run()` returns `ToolResult` with `parsed_output` | Uniform interface for all tool executors (nmap, httpx, subfinder, nuclei, ffuf). Agent loop only needs to know `run()` → `ToolResult`. S04 will replicate this pattern. | yes |
| 16 | 2026-03-13 | S03 | Prompt templating | Python f-strings, not Jinja2 | Spec says Jinja2 but it's an undeclared dep. S03 prompts need simple variable substitution only. Add Jinja2 when conditionals/loops needed (S05+). | yes |
| 17 | 2026-03-13 | S03 | Event emission pattern | Protocol with typed dataclasses | `AgentEventCallback` Protocol with `async on_event()`. Typed event dataclasses for each event kind. Most Pythonic, testable with mock, S05 implements with Rich rendering. | yes |
| 18 | 2026-03-13 | S03 | Tool output fed to LLM | JSON-serialized parsed_output, truncated to 4000 chars | Structured data is smaller and more useful for reasoning than raw stdout. Falls back to truncated stdout when parsed_output is None. | yes |
| 19 | 2026-03-14 | S04 | Tool-suite proof targets | Use an in-sandbox Python HTTP fixture plus repo fixtures for `httpx`/`nuclei`/`ffuf`, and treat `subfinder` as the only internet-gated proof with clean skip behavior | Removes external drift from most of S04's verification, keeps proofs deterministic, and narrows flaky runtime dependencies to the one tool that inherently needs a public domain. | yes |
| 20 | 2026-03-14 | S04 | Deterministic HTTP fixture runtime | Copy tool-suite proof assets into `/tmp/oxpwn-tool-suite`, serve them on port `18080`, and install full `python3` alongside `python3-minimal` in the sandbox image | `python3-minimal` alone lacks the stdlib `http` modules needed for `http.server`/`urllib`; a fixed in-container asset root + port makes future `httpx`/`nuclei`/`ffuf` proofs deterministic and debuggable. | yes |
| 21 | 2026-03-15 | S04 | `httpx` sandbox invocation | Feed targets to `httpx` over stdin via `sh -lc` heredoc, not via `-u` or positional args | The Kali-packaged `httpx-toolkit` build in the sandbox does not support `-u`, and positional targets produced no results; stdin-backed JSONL execution is the reliable multi-target path for S04/T04 integration. | yes |
| 22 | 2026-03-15 | S04 | Scanning tool output control | Run `nuclei` in JSONL mode with `-omit-raw -omit-template`, and run `ffuf` in stdout `-json` mode with ANSI stripping plus a default in-sandbox wordlist path | `nuclei` can bloat findings with raw request/response and template payloads, while `ffuf`'s machine-readable stdout base64-encodes fuzz inputs and still emits terminal control sequences. This contract keeps `parsed_output` compact for the agent while preserving raw stdout/stderr for audit. | yes |
| 23 | 2026-03-14 | S04 | Agent-visible five-tool suite | Register `nmap`, `httpx`, `subfinder`, `nuclei`, and `ffuf` in a stable default order with curated typed schemas, and steer Recon/Scanning prompts toward the phase-appropriate subset | The ReAct loop now sees a consistent schema inventory, multi-target executors stay usable without raw flag passthrough, and prompt guidance nudges the model toward the broader suite instead of falling back to `nmap` only. | yes |
| 24 | 2026-03-14 | S04 | `subfinder` integration proof gating | Preflight outbound connectivity before the public-domain `subfinder` proof and skip cleanly only when network access is unavailable | `subfinder` is the lone S04 tool that inherently depends on external passive sources; explicit preflight + skip messaging keeps the slice deterministic without masking genuine parser or executor regressions. | yes |
| 25 | 2026-03-14 | S05 | Streaming CLI fidelity | Use append-only Rich rendering at the CLI layer, but satisfy raw-output streaming with an additive `DockerSandbox.execute_stream()` + `ToolOutputChunkEvent` path that preserves buffered `ToolResult` parsing | R004 explicitly calls for real-time reasoning and raw tool output; additive chunk streaming avoids breaking the S02/S04 executor contract while keeping S06 on the same CLI surface | yes |
| 26 | 2026-03-14 | S05 | Pre-wizard scan bootstrap | Make `0xpwn scan` use `--target` plus env/option-backed advanced runtime inputs until S06 supplies YAML/wizard defaults | S05 needs a usable CLI before the first-run wizard exists; this keeps the user-facing command shape stable while letting S06 swap in persisted defaults later | yes |
| 27 | 2026-03-14 | S05 | Streaming opt-in boundary | Always pass an additive `output_sink` from the agent, but only inject it into executors whose `run()` signature accepts `output_sink` (or `**kwargs`) while `DockerSandbox.execute_stream()` buffers the same final stdout/stderr it streams | This keeps existing non-streaming executors and callers unchanged for T01, lets T02 migrate each tool incrementally, and preserves one final buffered `ToolResult` contract across streamed and non-streamed execution. | yes |
| 28 | 2026-03-15 | S06 | Config resolution precedence | CLI options > env vars > YAML config > wizard trigger — only raise `ScanBootstrapError` after wizard is skipped/declined/failed | Preserves existing env-backed behavior (Decision 26) while making YAML config a seamless fallback; wizard is the last resort, not a forced gate. Non-interactive terminals skip wizard entirely and get the existing error path. | yes |
| 29 | 2026-03-15 | S06 | Config file path convention | `$OXPWN_CONFIG` > `$XDG_CONFIG_HOME/oxpwn/config.yaml` > `~/.config/oxpwn/config.yaml`, atomic writes with `0o600` permissions | XDG compliance without a new dependency (`platformdirs`); atomic rename prevents partial writes on Ctrl+C; restrictive permissions protect API keys on shared systems. | no |
| 30 | 2026-03-15 | S06 | Wizard synchronous design | Wizard uses synchronous Rich prompts + httpx sync for Ollama probe; LLM validation uses `asyncio.run(litellm.acompletion())` for the single connectivity check | Wizard is an interactive blocking flow — async adds complexity with no benefit. The single async call is wrapped minimally to reuse the existing LiteLLM async API. | yes |
